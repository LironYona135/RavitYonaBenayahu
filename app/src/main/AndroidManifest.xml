<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.belove">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/app_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/app_logo_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BeLove">
        <activity
            android:name=".LoginRegisterActivity"
            android:exported="false"
            android:label="@string/app_name"
            android:theme="@style/Theme.BeLove.NoActionBar"/>

        <activity
            android:name=".ui.loginRegister.login.GoogleSignInActivity"
            android:exported="false" />
        <activity
            android:name=".ui.App.MainActivity"
            android:exported="false"
            android:label="@string/title_activity_rest_of_app" />
        <activity
            android:name=".ui.SplashPage.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.BeLove.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

        </activity>
    </application>

</manifest>


